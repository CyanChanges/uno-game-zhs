1. ~~使用 localstorage 和 sessionstorage 和 跨标签通讯, 让多个标签页可以同时加入大厅并且互不干扰~~
2. ✅ 在加入已经开始对局的大厅时, 可能显示 `[object Object]` 或报错 — 已修复: 服务端改用 `errorKey` 发送错误, 客户端查 `/errors` 获取消息; `message.message` 回退到 string 安全判断
3. ✅ 用户名/大厅名称存储时机 — 已添加 `input` 事件监听, 输入时即时存储到 localStorage
4. 部分情况下邀请 AI 按钮消失 (可能是房主状态判断和 UI 不同步的问题)
5. ✅ >2 人认输不再让对手胜利 — 移除认输玩家，手牌进弃牌堆，游戏继续；2 人房保持原行为（对手胜）
6. 观战模式: 在加入已经开始的房间时提示是否要进入观战模式
7. ✅ 在对局内显示玩家出牌顺序 — 服务端 start/update 消息增加 `direction` 字段; 客户端显示方向箭头和回合顺序
8. ✅ +2/+4 不再跳过受害者回合 — 受害者抽牌后轮到其正常出牌; skip 牌保持跳过行为不变
